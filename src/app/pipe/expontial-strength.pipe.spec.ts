import { ExpontialStrengthPipe } from './expontial-strength.pipe';

describe('ExpontialStrengthPipe', () => {
  it('create an instance', () => {
    const pipe = new ExpontialStrengthPipe();
    expect(pipe).toBeTruthy();
  });
});
